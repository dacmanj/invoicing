    <%= f.simple_fields_for :addresses do |a| %>
	    <%= a.input :address_lines %>
	    <%= a.input :city %>
	    <%= a.input :state %>
	    <%= a.input :zip %>
	    <%= a.input :email %>
	    <%= a.input :phone %>
	    <%= a.input :fax %>
    <% end %>