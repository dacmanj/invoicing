<%= simple_format [(contact.account.name unless contact.suppress_account_name), (contact.name unless contact.suppress_contact_name), contact.title, contact.address.address_lines, contact.address.city_state_zip, contact.address.email].reject{ |c| c.blank?}.join("\n")) unless (contact.blank? or contact.address.blank? %>