<%= simple_format [contact.account.name, (contact.name unless contact.account.name == contact.name), contact.title, contact.address.address_lines, contact.address.city_state_zip, contact.address.email].reject{ |c| c.blank?}.join("\n")) unless (contact.blank? or contact.address.blank? %>